name: CI

on: 
  push: 
    branches:
      - master

jobs:
  CI:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@master

      - name: Test
        run: echo ${{github.event.head_commit.message}}